PORT=3000

# Set to 'true' in production to actually send FCM push notifications
SEND_FCM=false

# Admin dashboard access token — generate with: node -e "require('crypto').randomBytes(32).toString('hex')|console.log"
ADMIN_TOKEN=your_random_token_here
